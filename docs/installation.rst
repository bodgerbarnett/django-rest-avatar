============
Installation
============

At the command line::

    $ easy_install django-rest-avatar

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rest-avatar
    $ pip install django-rest-avatar
